<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <!-- Link CSS của Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Link CSS của Add_edit_category.ejs -->
    <link rel="stylesheet" href="/styles/add_edit_category.css">
    <!-- CSS tùy chỉnh cho sidebar -->
    <style>
        /* Định dạng menu bên trái */
        .sidebar {
            height: calc(100vh - 65px);
            background-color: #343a40;
            color: #fff;
            overflow-y: auto;
        }
        .sidebar ul {
            list-style-type: none;
            padding-left: 0;
        }
        .sidebar li {
            padding: 10px 0;
        }
        .sidebar a {
            text-decoration: none;
            color: #fff;
        }
        /* Định dạng nội dung bên phải */
        .content {
            height: calc(100vh - 65px); /* Đặt chiều cao thành 100% của viewport height */
            padding: 20px;
            overflow-y: auto; /* Thêm thanh cuộn khi cần thiết */
        }
    </style>
</head>
<body>
    <!-- Header -->
    <%- include('../partials/admin_header') %>

    <!-- Main content -->
    <div class="container-fluid mt-1">
        <div class="row">
            <!-- Sidebar -->
            <div class="col-md-2 sidebar">
                <%- include('../partials/admin_sidebar') %>
            </div>
            <!-- Content -->
            <div class="col-md-10 content">
                <%- body %>
            </div>
        </div>
    </div>

    <!-- Link JS của Bootstrap -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
